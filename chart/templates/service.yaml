apiVersion: v1
kind: Service
metadata:
  name: {{ include "fbi-crime-analyst-ui.fullname" . }}
  labels:
    {{- include "fbi-crime-analyst-ui.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fbi-crime-analyst-ui.selectorLabels" . | nindent 4 }}
